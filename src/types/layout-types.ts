import {
  footerText,
  headerText,
  paginationText,
  searchBarText,
  sideBarText,
  sortText,
} from "@/data/layout-data";

export type headerTextModel = typeof headerText;

export type searchBarTextModel = typeof searchBarText;

export type footerTextModel = typeof footerText;

export type sideBarTextModel = typeof sideBarText;

export type sortTextModel = typeof sortText;

export type paginationTextModel = typeof paginationText;
